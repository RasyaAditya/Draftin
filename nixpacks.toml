[phases.setup]
nixPkgs = ["nodejs_20"]
nixLibs = []

[phases.install]
cmds = ["npm ci --prefix Backend-Draftin-Clean"]

[phases.build]
cmds = []

[start]
cmd = "cd Backend-Draftin-Clean && node index.js"
