[build]
builder = "NIXPACKS"
buildCommand = "cd Backend-Draftin-Clean && npm install"

[deploy]
startCommand = "cd Backend-Draftin-Clean && npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
